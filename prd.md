# 手持商品 MVP · PRD(接口规范级)

> M2 产出 · 2026-05-26 · 接口规范级
> 输入:M1 战略文档 · 4 个差异化支柱
> 输出 → M3 工程脚手架

---

## TL;DR(给开发的一页摘要)

| 项 | 内容 |
|---|---|
| **功能** | 千图AI 商品图功能加"手持版"tab,用户上传商品图 + 选模特(3 选 1)+ 生成 |
| **核心 API** | 4 个端点:获取模特列表 / 提交生成任务 / 查询任务结果 / 查询算力余额 |
| **核心埋点** | 12 个事件,支撑 3 个行为指标(使用率/完成率/复用率) |
| **算力消耗** | 与万相对齐:**12 算力值/次**(实际数字 TBD,等后端确认) |
| **生成 SLA** | 目标 ≤30s,P95 ≤45s |
| **入口** | 商品白底图功能页加 tab,**"手持版"**(显眼位置,不可折叠) |
| **MVP 范围** | 3 个 stock 模特 × 1 个固定姿势 × 3 种尺寸 |
| **不做** | 用户上传模特 / 参照物校准 / 多姿势 / 视频 |

---

## 一、项目元信息

| 字段 | 值 |
|---|---|
| 项目代号 | `handheld-product-mvp` |
| 前端 Branch | `feature/handheld-product-mvp`(千图AI 前端单仓) |
| 后端服务 | 复用现有 AI 算法服务(部分微服务) |
| 算法底模 | 评估 Seedream 4.5 vs Flux.2 pro(M3 POC 决定) |
| 开发周期 | 3 周(W1 算法 POC / W2 工程化 / W3 接入 + 灰度) |
| 观察周期 | 4 周(灰度 10%,跑 3 个指标) |
| 责任人 | 产品:白笑 · 开发:TBD · 设计:TBD |

---

## 二、用户故事(User Stories)

按 MoSCoW 优先级排:

### Must Have(MVP 必做)

| ID | 用户故事 | 验收标准 |
|---|---|---|
| **US-001** | 作为电商商家,我想在千图AI 商品图功能里看到"手持版"入口,这样我知道有这个功能 | 商品图功能页 tab 区域可见"手持版"标签,有 NEW 标识(MVP 期) |
| **US-002** | 作为电商商家,我想上传我的商品图,这样系统知道要 P 哪个商品 | 支持 JPG/PNG/WEBP,512-2048px,≤10MB;支持拖拽和点击上传 |
| **US-003** | 作为电商商家,我想从 3 个预设模特里选一个,这样我不用自己找模特素材 | 3 张模特缩略图,默认选中第 1 个,点击切换 |
| **US-004** | 作为电商商家,我想点一下"生成",系统自动出图,这样我不用写提示词 | 默认 3-4 步内可生成 |
| **US-005** | 作为电商商家,我想看到生成进度,知道还要等多久 | Loading 页显示百分比 + "预计 30 秒"文案 |
| **US-006** | 作为电商商家,我想下载生成的图,直接用 | 提供下载按钮,文件命名 `handheld_{timestamp}.jpg` |
| **US-007** | 作为电商商家,如果生成失败,我希望算力退还 | 失败自动退算力 + 提示重试 |
| **US-008** | 作为电商商家,如果算力不足,我希望被引导去充值 | 算力不足弹窗 + 充值按钮 |

### Should Have(MVP 建议做)

| ID | 用户故事 | 验收标准 |
|---|---|---|
| **US-101** | 作为电商商家,我想选输出尺寸(1:1 / 3:4 / 2:3),适配不同平台 | 三选一 radio,默认 1:1 |
| **US-102** | 作为电商商家,我想看到这次生成会消耗多少算力 | 生成按钮上显示"生成(12 算力)" |
| **US-103** | 作为电商商家,我想把生成结果保存到我的素材库 | 提供"保存到素材库"按钮 |

### Could Have(MVP 可做可不做)

| ID | 用户故事 |
|---|---|
| **US-201** | 作为电商商家,我想再次生成(同样商品 + 模特 + 尺寸) — "再生成"按钮 |
| **US-202** | 作为电商商家,我想复制生成图的 URL,分享给同事 |

### Won't Have(MVP 不做,留给 H2)

| ID | 用户故事 | 原因 |
|---|---|---|
| **US-901** | 上传自己的模特图 | 战略决策:不走 Krea 派 |
| **US-902** | 选具名模特(姓名/民族/年龄) | 肖像授权太贵 |
| **US-903** | 多姿势选择 | 1 个固定姿势 MVP 验证够用 |
| **US-904** | 参照物校准 | H2 完整版独占功能 |
| **US-905** | 批量生成多张 | 留 H2 |
| **US-906** | 自定义提示词/风格 | 中小商家用不到 |

---

## 三、用户流程

### 主流程(3 步默认路径)

```
[商品图功能页]
    ↓ 点击"手持版"tab
[手持版主页]
    ↓ 上传商品图(必填)
    ↓ 选模特(默认选中第 1 个,可不操作)
    ↓ 点"生成"
[Loading 页 0-30s]
    ↓ 完成
[结果页]
    ↓ 下载 / 再生成 / 保存到素材库
```

### 完整流程(含高级选项,4 步路径)

```
[商品图功能页] → 点击"手持版"tab
    ↓
[手持版主页]
    ├─ 上传商品图(必填)
    ├─ 选模特(3 选 1,默认第 1)
    └─ 高级选项(折叠,默认收起)
        ├─ 输出尺寸:1:1 / 3:4 / 2:3(默认 1:1)
        └─ 高清放大(默认关,开启 +X 算力)
    ↓ 点"生成"
[算力预校验]
    ├─ 充足 → 继续
    └─ 不足 → 弹窗"算力不足" + 跳转充值
    ↓
[Loading 页]
    ├─ 显示进度条 + 倒计时
    └─ 取消按钮(可取消,扣算力)
    ↓
[结果页]
    ├─ 大图预览
    ├─ 下载(PNG/JPG)
    ├─ 再生成(消耗算力)
    ├─ 保存到素材库
    └─ 返回主页
```

### 异常流程

| 异常 | 触发条件 | 处理 |
|---|---|---|
| 上传失败 | 文件 >10MB / 格式不支持 / 尺寸 <512px | 红色提示文案 + "重新上传"按钮 |
| 网络中断 | 上传/生成过程中断网 | 重连提示 + 自动恢复 + 失败重试 |
| 生成失败 | 算法返回失败 / 超时 60s | 自动重试 1 次,仍失败则提示 + 退算力 |
| 算力不足 | 余额 < 单次消耗 | 弹窗 + 充值按钮 + 留存当前操作 |
| 并发超限 | 同用户 >3 个并发生成 | 提示"上一个还在生成中,稍后再试" |

---

## 四、UI 规格(屏幕级)

### 屏 1 · 商品图功能页(入口)

**位置**:千图AI 商品图功能页顶部 tab 栏
**新增 tab**:`手持版`(放在现有 tab 之后,带 `NEW` 标识)

**视觉规格**(对接 design 项目的 design token):
- tab 字号:对接 `font/tab-default`
- tab 颜色:激活态 `color/accent/lime`,非激活 `fg/2`
- NEW 标识:背景 `accent/red-bg`,文字 `accent/red-fg`,字号 10px

### 屏 2 · 手持版主页

**布局**(从上到下,Section 划分):

```
┌─────────────────────────────────────────────────────────────┐
│  [Tab 切换条]  商品图 | 手持版(NEW)                          │
├─────────────────────────────────────────────────────────────┤
│  Section A · 上传商品图                                       │
│  ┌───────────────────────────────────┐                      │
│  │  拖拽 / 点击上传商品图              │                      │
│  │  JPG / PNG / WEBP · ≤10MB · 清晰图  │                      │
│  └───────────────────────────────────┘                      │
├─────────────────────────────────────────────────────────────┤
│  Section B · 选模特                                           │
│  [模特1·激活]  [模特2]  [模特3]                              │
│  缩略图卡片 256×256,激活态显示边框 accent/lime               │
├─────────────────────────────────────────────────────────────┤
│  Section C · 高级选项 ▾(默认收起)                            │
│  ├─ 输出尺寸:[1:1·默认] [3:4] [2:3]                          │
│  └─ 高清放大:◯ 关闭 ◯ 开启(+4 算力)                        │
├─────────────────────────────────────────────────────────────┤
│  [生成(12 算力)]  ← 主 CTA,使用 button/hero token         │
└─────────────────────────────────────────────────────────────┘
```

**状态规格**:

| 元素 | 默认 | Hover | Active | Disabled | Loading | Error |
|---|---|---|---|---|---|---|
| 上传区域 | 虚线框 + 提示文案 | 边框加深 | 显示已上传图 | — | 缩略图 + spinner | 红框 + 错误文案 |
| 模特卡片 | 边框 1px line/2 | 边框 1px line/3 | 边框 2px accent/lime + 勾选标 | 灰度 50% | — | — |
| 生成按钮 | button/hero/bg | button/hero/bg-hover | button/hero/bg-active | button/disabled-bg | spinner + "生成中..." | — |

### 屏 3 · Loading 页

**布局**:Modal 居中弹出,不可关闭(可取消)

```
┌──────────────────────────────────┐
│                                  │
│       [Loading Spinner]          │
│                                  │
│       生成中 65%                  │
│       预计还需 12 秒              │
│                                  │
│       [取消生成]                  │
│                                  │
└──────────────────────────────────┘
```

**进度条逻辑**:
- 0-10%:任务排队
- 10-80%:算法生成中(模拟线性增长)
- 80-95%:后处理
- 95-100%:回传结果

### 屏 4 · 结果页

**布局**:

```
┌─────────────────────────────────────────────────────────────┐
│  [返回]                                                       │
│                                                              │
│         ┌──────────────────────┐                            │
│         │                       │                            │
│         │    生成结果大图        │                            │
│         │                       │                            │
│         └──────────────────────┘                            │
│                                                              │
│  [下载 PNG]  [下载 JPG]  [再生成(12 算力)]  [保存到素材库]  │
│                                                              │
│  反馈:这次效果 ◯ 满意 ◯ 一般 ◯ 不满意(选项埋点)            │
└─────────────────────────────────────────────────────────────┘
```

### 屏 5 · 错误页 / 弹窗

| 错误类型 | 弹窗标题 | 文案 | 按钮 |
|---|---|---|---|
| 上传文件过大 | 上传失败 | 文件超过 10MB,请压缩后重试 | 重新上传 |
| 格式不支持 | 格式不支持 | 请上传 JPG / PNG / WEBP 格式 | 重新上传 |
| 算力不足 | 算力不足 | 本次消耗 12 算力,余额 X 算力 | 充值算力 / 取消 |
| 生成失败 | 生成失败 | 算法出错,已退还算力,可重试 | 重试 / 返回 |
| 网络异常 | 网络异常 | 请检查网络后重试 | 重试 |

---

## 五、接口规范(核心 4 个 API)

> Base URL:`https://api.qiantu.ai/v1/handheld`
> 鉴权:复用千图AI 现有 JWT token
> Content-Type:`application/json`

### API-1 · 获取模特列表

```
GET /api/v1/handheld/models
```

**请求**:无参数

**响应**:
```json
{
  "code": 0,
  "data": {
    "models": [
      {
        "id": "model_001",
        "name": "模特 A",
        "thumbnailUrl": "https://cdn.qiantu.ai/handheld/models/001_thumb.jpg",
        "previewUrl": "https://cdn.qiantu.ai/handheld/models/001_preview.jpg",
        "gender": "female",
        "ethnicity": "asian",
        "pose": "holding_front",
        "isDefault": true,
        "order": 1
      },
      { "id": "model_002", ... },
      { "id": "model_003", ... }
    ]
  }
}
```

**字段说明**:

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` | string | ✅ | 模特唯一 ID |
| `name` | string | ✅ | 展示名 |
| `thumbnailUrl` | string | ✅ | 列表用缩略图(256×256) |
| `previewUrl` | string | ✅ | 详情用大图(512×512) |
| `gender` | enum | ✅ | `female` / `male`(MVP 全 female) |
| `ethnicity` | enum | ✅ | `asian` / `western` / `other`(MVP 全 asian) |
| `pose` | enum | ✅ | `holding_front` 等(MVP 全 `holding_front`) |
| `isDefault` | bool | ✅ | 是否默认选中(同时仅 1 个为 true) |
| `order` | int | ✅ | 排序权重,小的在前 |

---

### API-2 · 提交生成任务

```
POST /api/v1/handheld/generate
```

**请求**:
```json
{
  "productImageUrl": "https://upload.qiantu.ai/temp/abc123.jpg",
  "modelId": "model_001",
  "aspectRatio": "1:1",
  "advanced": {
    "hdEnhance": false
  }
}
```

**字段说明**:

| 字段 | 类型 | 必填 | 约束 |
|---|---|---|---|
| `productImageUrl` | string | ✅ | 必须是千图AI 自家 CDN(上传走另一个 API,见 API-5 备用) |
| `modelId` | string | ✅ | 必须是 API-1 返回的有效 ID |
| `aspectRatio` | enum | ✅ | `1:1` / `3:4` / `2:3` |
| `advanced.hdEnhance` | bool | ❌ | 默认 false,开启增加 4 算力 |

**响应**:
```json
{
  "code": 0,
  "data": {
    "taskId": "task_xxxxxxxxxxxx",
    "estimatedSeconds": 25,
    "cost": 12
  }
}
```

**错误码**:

| code | 说明 | HTTP |
|---|---|---|
| 0 | 成功 | 200 |
| 40001 | 参数错误 | 400 |
| 40002 | productImage 格式不支持 | 400 |
| 40101 | 鉴权失败 | 401 |
| 40301 | 算力不足 | 403 |
| 40901 | 并发超限(同用户 >3 个并发) | 409 |
| 50001 | 算法服务异常 | 500 |
| 50002 | 算法服务超时 | 504 |

---

### API-3 · 查询任务结果

```
GET /api/v1/handheld/result/{taskId}
```

**请求**:URL path 中带 `taskId`

**响应**:
```json
{
  "code": 0,
  "data": {
    "taskId": "task_xxxxxxxxxxxx",
    "status": "success",
    "progress": 100,
    "resultUrl": "https://cdn.qiantu.ai/handheld/results/xxx.jpg",
    "createdAt": 1717000000,
    "completedAt": 1717000025,
    "cost": 12,
    "refunded": false
  }
}
```

**status 枚举**:

| status | 说明 |
|---|---|
| `pending` | 排队中(尚未开始算法处理) |
| `processing` | 算法生成中 |
| `success` | 成功 |
| `failed` | 失败(自动退算力,`refunded: true`) |
| `cancelled` | 用户取消 |

**轮询策略**:
- 前端每 1 秒轮询一次
- 状态为 `success` / `failed` / `cancelled` 时停止轮询
- 超过 60 秒仍 `processing` → 前端主动取消并提示

---

### API-4 · 查询算力余额

```
GET /api/v1/handheld/quota
```

**请求**:无参数

**响应**:
```json
{
  "code": 0,
  "data": {
    "currentBalance": 86,
    "costPerGeneration": 12,
    "costHdEnhance": 16,
    "sufficient": true
  }
}
```

**用途**:生成按钮显示"生成(12 算力)"时调用,以及算力不足时弹窗信息。

---

### API-5 · 上传商品图(备用,如千图AI 已有上传接口则复用)

```
POST /api/v1/handheld/upload
Content-Type: multipart/form-data
```

**请求**:`file` 字段为图片二进制

**响应**:
```json
{
  "code": 0,
  "data": {
    "url": "https://upload.qiantu.ai/temp/abc123.jpg",
    "expireAt": 1717003600
  }
}
```

**约束**:
- 文件大小 ≤10MB
- 格式:JPG / PNG / WEBP
- 尺寸:512-2048px(长边)
- 临时存储 1 小时,过期后自动删除

---

## 六、埋点规范(12 个事件)

> 埋点平台:复用千图AI 现有埋点(假设是神策/友盟/自研,TBD)
> 用户属性:复用千图AI 现有用户标签体系,**必须能区分"电商用户" vs 非电商用户**

### 事件清单

| 事件名 | 触发时机 | 关键属性 |
|---|---|---|
| `handheld_entry_view` | 商品图功能页加载,手持 tab 可见 | userId, userType, sessionId |
| `handheld_tab_click` | 点击手持 tab | userId, userType, sessionId, previousTab |
| `handheld_upload_start` | 用户点击/拖拽开始上传 | userId, sessionId, fileSize, fileType |
| `handheld_upload_success` | 上传 API 200 返回 | userId, sessionId, fileSize, durationMs |
| `handheld_upload_fail` | 上传失败 | userId, sessionId, errorCode, errorMessage |
| `handheld_model_select` | 用户切换模特 | userId, sessionId, modelId |
| `handheld_advanced_expand` | 用户展开"高级选项" | userId, sessionId |
| `handheld_generate_click` | 点击生成按钮 | userId, sessionId, modelId, aspectRatio, hdEnhance, cost |
| `handheld_generate_success` | 任务 status 变 success | userId, sessionId, taskId, durationMs, cost |
| `handheld_generate_fail` | 任务 status 变 failed | userId, sessionId, taskId, errorCode, durationMs |
| `handheld_download` | 用户点下载按钮 | userId, sessionId, taskId, format |
| `handheld_regenerate` | 用户点再生成按钮 | userId, sessionId, taskId |
| `handheld_feedback` | 用户在结果页点反馈 | userId, sessionId, taskId, rating (`satisfied` / `neutral` / `unsatisfied`) |

### 3 个核心指标的计算

| 指标 | 计算公式 |
|---|---|
| **使用率** | UV(`handheld_tab_click`,userType=电商)/ UV(商品图功能页,userType=电商) |
| **完成率** | Σ(`handheld_generate_success`)/ Σ(`handheld_tab_click`)(去重 sessionId) |
| **复用率** | 7 天滚动窗口内,触发 ≥2 次 `handheld_generate_success` 的用户 / ≥1 次的用户 |

### 数据看板(M2 PRD 阶段提需求)

埋点上线后需要数据团队搭建:
1. 实时漏斗图(入口 → tab 点击 → 上传 → 生成成功)
2. 3 指标日报(自动发到产品群)
3. 失败原因分布(`upload_fail` / `generate_fail` 的 errorCode 统计)

---

## 七、入口位置(写死)

**强制规范**:

- 位置:千图AI 商品图功能页 tab 栏
- 位序:**现有 tab 之后,作为最后一个 tab**(不抢占现有功能)
- 标识:`手持版` + `NEW` 红点(MVP 灰度期间常驻,稳定后撤)
- 不可折叠:任何时候,不允许将"手持版"tab 折叠/隐藏/二级菜单化
- 移动端:同样在 tab 栏可见

**反例(开发不能做)**:
- ❌ 放在"更多"折叠菜单里
- ❌ 放在侧边栏二级
- ❌ 用户首次进入加引导浮层(MVP 不做引导,看裸数据)
- ❌ 自动跳转到手持 tab(必须用户主动点)

---

## 八、算力计费对接

### 计费规则

| 操作 | 算力消耗 |
|---|---|
| 单次生成(标准) | 12 算力 |
| 单次生成 + HD 增强 | 16 算力 |
| 失败/取消 | **退还 100%** |

### 对接流程

```
[用户点生成]
    ↓
[前端调 API-4 查余额]
    ↓
[余额 ≥ 12]
    ├─ 是 → 调 API-2 提交任务(后端扣算力)
    └─ 否 → 弹窗"算力不足" + 跳转充值
    ↓
[任务结果]
    ├─ success → 算力实扣
    └─ failed/cancelled → 后端自动退还
```

### 待后端确认(TBD)

| # | 待确认项 | 谁问 | 影响 |
|---|---|---|---|
| 1 | 千图AI 算力体系是否支持"按任务粒度扣 + 失败退还"? | 后端 | 直接影响 API-2/API-3 实现 |
| 2 | 12 算力 = 多少人民币? | 商业团队 | 影响营销文案 |
| 3 | 算力余额接口是否已有,还是新建? | 后端 | 影响 API-4 是否真的要新建 |

---

## 九、边界情况与错误处理

### 上传环节

| 边界 | 处理 |
|---|---|
| 文件 >10MB | 前端拒绝 + 提示压缩 |
| 长边 <512px | 前端拒绝 + 提示"图片过小" |
| 长边 >2048px | 前端自动压缩到 2048px |
| 非 JPG/PNG/WEBP | 前端拒绝 + 提示格式 |
| HEIC 格式 | 前端自动转 JPG(参考 Krea.ai) |
| 拖入多个文件 | 只接受第 1 个,提示"只支持单张" |
| 损坏的文件 | 后端校验失败 + 退算力 + 前端提示 |

### 生成环节

| 边界 | 处理 |
|---|---|
| 同用户并发 >3 | 后端拒绝 + 提示"上一个还在生成中" |
| 算法 60s 未返回 | 前端主动取消任务 + 退算力 + 提示重试 |
| 算法返回低质量(blurry/扭曲) | MVP 不做自动质检,依赖用户反馈 `handheld_feedback` |
| 模特库为空 | 应不出现;若出现,前端展示空状态 + "联系客服" |

### 网络环节

| 边界 | 处理 |
|---|---|
| 上传中断网 | 自动重试 3 次,失败提示 |
| 轮询中断网 | 重连后从最新状态继续 |
| API 5xx | 自动重试 1 次,仍失败展示通用错误页 |

---

## 十、验收标准(QA Checklist)

### 功能验收

- [ ] US-001 到 US-008(Must Have)全部通过
- [ ] US-101 到 US-103(Should Have)至少通过 2/3
- [ ] 异常流程的 5 个错误类型都能正确触发并展示对应文案
- [ ] 跨浏览器:Chrome / Safari / Edge 主流版本
- [ ] 移动端:tab 可见,可点击,主流程可走通(MVP 不做移动端优化,但必须可用)

### 性能验收

| 指标 | 阈值 |
|---|---|
| 上传一张 2MB 图 | ≤3s(P95) |
| 提交生成任务 API 响应 | ≤500ms(P95) |
| 轮询查询 API 响应 | ≤200ms(P95) |
| 端到端生成时间(用户点击 → 结果展示) | **≤30s(P50),≤45s(P95)** |
| 首屏加载(从商品图页点击 tab 到主页可交互) | ≤1.5s(P95) |

### 埋点验收

- [ ] 12 个事件全部能正常上报(用 Chrome DevTools Network 验证)
- [ ] 事件属性齐全,无空字段
- [ ] 用户标签 `userType=电商` 正确赋值(关键,否则使用率算不准)
- [ ] 数据看板能看到实时漏斗

### 数据验收(灰度期)

- [ ] 灰度 10% 用户,观察 48 小时
- [ ] 3 个核心指标的数据采集是否准确(对照原始事件 vs 计算指标)
- [ ] 错误率 <5%(`generate_fail` / `generate_click`)

---

## 十一、性能指标(SLA)

| 维度 | 目标 | 上限 |
|---|---|---|
| 单次生成时延(P50) | 25s | 35s |
| 单次生成时延(P95) | 40s | 60s |
| API 可用性 | 99.5% | — |
| 失败率(generate_fail / generate_click) | <3% | <5% |

---

## 十二、上线灰度策略

| 阶段 | 灰度比例 | 时长 | 观察重点 |
|---|---|---|---|
| 内测 | 团队内 + KOL 商家 10 人 | 2 天 | 流程跑通 + 出图质量 |
| 灰度 A | 10% 电商用户 | 1 周 | 3 个指标初步数据 + 异常率 |
| 灰度 B | 50% 电商用户 | 1 周 | 3 个指标稳定性 + 算力消耗规模 |
| 全量 | 100% 电商用户 | — | 进入观察期,4 周后决断 H2 |

**回滚条件**:
- 失败率 >10% → 立即回滚
- 算力异常消耗(同用户单日 >100 算力)→ 限流或回滚
- 出图质量集体投诉 → 限流 + 算法回滚

---

## 十三、TBD(待 M2 中段补齐)

| # | 待确认项 | 谁来补 | 影响范围 | 状态 |
|---|---|---|---|---|
| 1 | 千图AI 现有商品图功能的月活/复用率(**项目级 gate**) | 数据团队 | 决定项目是否继续 | ⏳ |
| 2 | 千图AI 算力计费体系是否支持"按任务粒度扣 + 失败退还" | 后端团队 | 影响 API-2/API-3 实现 | ⏳ |
| 3 | 12 算力 = 多少人民币(为营销文案准备) | 商业团队 | 影响 UI 文案 | ⏳ |
| 4 | 千图AI 现有 AI 算法服务支持的底模列表 | 算法团队 | 影响 M3 POC 选型 | ⏳ |
| 5 | 3 个 stock 模特素材来源(采购站 / 自拍 / 合成) | 设计同事 | 影响 M3 工程实施 | ⏳ |
| 6 | 千图AI 现有上传接口是否可复用(API-5 是否新建) | 后端团队 | 减少工程量 | ⏳ |
| 7 | 千图AI 现有埋点平台(神策/友盟/自研) | 数据团队 | 影响埋点 SDK 调用方式 | ⏳ |
| 8 | "电商用户"标签的定义(GMV?店铺关联?) | 产品+数据 | 影响使用率指标计算 | ⏳ |

---

## 十四、本文档迭代记录

| 日期 | 版本 | 更新内容 |
|---|---|---|
| 2026-05-26 | v0.1 | M2 PRD 初稿:8 用户故事 + 5 API + 12 埋点 + 5 屏 UI + 8 TBD |
